# Новый администратор admin@LK.com

## Что будет сделано

1. **Создание учётной записи администратора**
   - Email: `admin@LK.com`, пароль: `7778LK`.
   - Аккаунт создаётся сразу подтверждённым (без письма), профиль ФИО — «Администратор».
   - Роль `admin` присваивается в таблице ролей.

2. **Снятие прав у текущего админа**
   - У `nkor777802@gmail.com` убирается роль `admin`.
   - Аккаунт остаётся в системе как обычный сотрудник (роль `employee`), доступ к `/admin` закрывается.

3. **Удаление страницы `/bootstrap`**
   - Удаляется маршрут `src/routes/bootstrap.tsx`.
   - Удаляется серверная функция `bootstrapFirstAdmin` из `src/lib/admin.functions.ts` (и связанный код в `admin.server.ts`, если он там есть).
   - Проверяется, что нигде не осталось ссылок на `/bootstrap`.

4. **Проверка**
   - Вход под `admin@LK.com` / `7778LK`, открытие `/admin` и раздела «Пользователи».
   - Подтверждение, что старый аккаунт больше не видит админ-панель.

## Технические детали

- Создание пользователя выполняется через Auth Admin API (сервисный ключ) одноразовым серверным вызовом, затем строка `admin` добавляется в `public.user_roles`.
- Пароль `7778LK` — 6 символов; если политика требует минимум 8, потребуется либо расширить пароль, либо снизить порог. Уточню/подстрою при выполнении.
- Изменение ролей — операция с данными (`user_roles`), схема БД не меняется.

## Замечание по безопасности

Пароль `7778LK` короткий и передан в переписке — рекомендую сменить его после первого входа через раздел профиля.
