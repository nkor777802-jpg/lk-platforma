# Убрать фото завода из hero на «О предприятии», сохранить синий фон на главной

## Что меняем

1. **Страница `/about` (`src/routes/_public/about.tsx`)**
   - Удалить импорт `plantExterior` и константу `PLANT_IMAGE`.
   - Убрать `<figure>` с фотографией завода и оранжевую акцентную плашку (`bg-primary`) из hero-блока.
   - Вернуть hero к одноколоночному текстовому layout'у на фоне `brand-pattern-blue`.
   - Удалить `og:image` и `twitter:image` из `head()`, так как на странице больше нет обложечного изображения.
   - Сохранить остальные SEO-мета и canonical.

2. **Главная страница `/` (`src/routes/_public/index.tsx`)**
   - Не трогать: hero остаётся с `brand-pattern-blue` и текущим содержимым.

3. **Проверка**
   - Сборка проходит без ошибок.
   - На `/about` нет фото завода, фон — синий паттерн.
   - На `/` синий паттерн в hero сохранён.
